import React from "react";
import Payment from "../components/Payment";

// Simulação de dados, no seu código real isso viria do backend
const qrCode = "BASE64_ENCODED_QR_CODE";  // QR Code gerado em base64
const paymentHash = "EXAMPLE_PAYMENT_HASH";  // payment_hash gerado
const user = "user1";  // Usuário logado

const PaymentPage: React.FC = () => {
  return (
    <div>
    </div>
  );
};

export default PaymentPage;
